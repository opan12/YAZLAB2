<h2>Şifremi Unuttum</h2>
<p>E-posta adresinize şifre sıfırlama onay kodu gönderildi.</p>
@if (TempData["Message"] != null)
{
    var messageType = TempData["MessageType"]?.ToString() ?? "info"; // Varsayılan mesaj tipi "info"
    <div class="alert alert-@messageType">
        @TempData["Message"]
    </div>
}
